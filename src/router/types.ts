export enum RouteUrls {
    HOME = '/',
    EVENT_LOOP = '/event-loop',
    NOT_FOUND = '/:pathMatch(.*)*',
}

export enum RouteNames {
    HOME = 'Home',
    EVENT_LOOP = 'EventLoop',
    NOT_FOUND = 'NotFound',
}
